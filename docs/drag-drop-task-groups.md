# Drag and drop task groups

PlushLife supports moving tasks between task groups without opening Edit. The implementation is touch-first, supports mouse drag and drop, saves immediately, and shows an undo affordance. This document is generated alongside the implementation and can be removed once the behavior is covered by the main help content.
